---
role_name_check: 1
dependency:
  name: galaxy
driver:
  name: podman
platforms:
  - name: instance
    image: "quay.io/rockylinux/${MOLECULE_DISTRO}"
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
